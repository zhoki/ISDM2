New task!

<%= "#{@user.firstName} #{@user.lastName}" %>, 

We are delighted to inform you that you are assigned to work on a document called "<%= @task.document_template.name %>" for project "<%= @project.name %>".

For more information, please visit this link: <%= url_for(controller: 'tasks', action: 'index', only_path: false) %>


May the odds be ever in your favor, 
PDMS 
